import 'package:flutter/material.dart';

class AppColors {
  // Primäre Farben
  static const primaryColor = Color(0xFF2E7D32); // Dunkelgrün
  static const primaryLightColor = Color(0xFF81C784); // Hellgrün
  static const accentColor = Color(0xFF4CAF50); // Mittleres Grün

  // Hintergrundfarben
  static const backgroundColor = Color(0xFFFFFFFF);
  static const darkBackgroundColor =
      Color(0xFF193E1C); // Dunkelgrün für Hintergrund

  // Text- und Symbolfarben
  static const textColor = Color(0xFF333333);
  static const lightTextColor = Color(0xFF757575);

  // Statusfarben
  static const errorColor = Color(0xFFD32F2F);
  static const successColor = Color(0xFF388E3C);
  static const warningColor = Color(0xFFFFA000);

  // Gradienten Farben für den Hintergrund
  static const gradientStart = Color(0xFF2E7D32); // Dunkelgrün
  static const gradientEnd = Color(0xFF1B5E20); // Sehr dunkelgrün
}
